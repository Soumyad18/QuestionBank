package com.qb.core.enums;

/**
 * Interview round types.
 * Stored as lowercase in DB (e.g., "l1", "screening").
 */
public enum Round {
    SCREENING, L1, L2, L3, L4, L5, L6, L7, L8, L9, L10, L11, L12, HR, F2F, TECHNICAL, MANAGERIAL
}
